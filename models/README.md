# Trained Models

The trained model checkpoints are not included in the GitHub repository.

Place the following files in this directory to run the speech recognition demo locally:

- `baseline_model.pt`
- `augmented_model.pt`

The models are trained using the notebook included in this repository.